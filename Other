		//不使用第三個參數，將int a 跟 int b的值互換。
		int a=5;
		int b=3;
		
		a = Integer.valueOf((String.valueOf(a)+b));
		b = Integer.valueOf((String.valueOf(a)+b).substring(0, 1) );
		a = Integer.valueOf((String.valueOf(a)+b).substring(1, 2) );

		System.out.println("a="+a);
		System.out.println("b="+b);
